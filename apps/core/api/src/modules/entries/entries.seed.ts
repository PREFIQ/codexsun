export async function seedEntriesModule() {
  return {
    seeded: 0,
    source: "billing-entry-workflow",
    message: "Entries are created by tenant billing workflows and do not need platform seed data."
  };
}
