{
  "name": "@codexsun/core-api",
  "version": "1.0.8",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ../../../tools/preflight.mjs core-api",
    "build": "tsc -p tsconfig.json",
    "typecheck": "npm run build -w @codexsun/framework && tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@codexsun/framework": "^1.0.8",
    "fastify": "^5.2.1",
    "kysely": "^0.29.0",
    "mysql2": "^3.22.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
