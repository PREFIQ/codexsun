export type PhoneBlock = {
  phoneId: string;
  label: string;
  number: string;
  isPrimary: boolean;
};

export type EmailBlock = {
  emailId: string;
  label: string;
  address: string;
  isPrimary: boolean;
};
